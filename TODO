Mask bottom as well as top

+ repurpose prep_mask_manual to interactively select training data
    + write prep_mask_labels(poly1, poly2, ...) to create a labels for training from polygons
    + write prep_mask_define_labels(rgb) to interactively make polygons for labeled training data
    + figure out where to put the pack and unpack utilities - standalone in util?


---

+ find decent parameters for 01 siden (and testcase 00)
+ branch off to test multiband xcorr
+ if improves results, then drop adaptive equalization and update accordingly

---

+ test Mark's padding routine
+ test my padding routine
+ prepare a nice results script he can run
+ this to be sent to him.

---

+ spline2 requires MEX file be built, how to make this easier?

